# TaskTree 开发记录 📝

本项目用于记录开发进度及里程碑达成情况。

---

## 阶段一：基础架构搭建
**日期**: 2026-04-11 (第1天)

### 🧱 核心完成内容
1. **项目初始化**
   - 创建项目目录结构 `E:\Project\TaskTree\`
   - 构建前后端基础代码骨架 (Vite + React / FastAPI + SQLAlchemy)

2. **核心文档编写**
   - 需求文档、项目计划、技术方案、数据库设计、API 接口定义等。

3. **后端基础开发**
   - 完成数据库引擎、安全认证 (JWT)、路由组装及 9 大表结构的 SQLAlchemy 模型定义。
   - 完成了包含项目、任务、用户等基础 CRUD 的 RESTful API 开发。

4. **前端基础开发**
   - 配置 Tailwind CSS 和 Ant Design UI 库。
   - 完成页面基础路由封装及状态管理库 (Zustand) 的配置。
   - 实现了注册、登录、项目列表等基础核心页面。

5. **代码审查与基础优化**
   - 修复 JWT 类型不匹配错误、优化延时加载及树状结构查询性能。

---

## 阶段二/三：高级视图与扩展功能开发
**日期**: 2026-04-11 (第2天 上午/下午)

### 🚀 核心完成内容
1. **多维度视图系统重构**
   - **任务树拖拽**: 引入 `@dnd-kit` 实现复杂的无限层级无缝拖拽重排与父子节点转移。
   - **看板视图**: 提供按状态划分的看板展示模式 (`KanbanView.tsx`)。
   - **列表与甘特图**: 增加 AntD Table 处理数据报表；接入 `gantt-task-react` 处理任务时间线。
   - **依赖关系图**: 引进 `ReactFlow` 实现任务依赖链路的高级可视化。

2. **任务与标签系统强化**
   - **侧边控制台**: 开发 `TaskDetailDrawer.tsx` 抽屉，实现任务各属性的实时内联编辑。
   - **评论与集成**: 侧边台增加评论与 @ 推送逻辑 (`CommentList.tsx`)。
   - **多阶标签**: 完成全局项目级别彩色标签（跨任务交叉筛选功能底层预留）。

3. **进阶数据流转**
   - **消息轮询**: 建立基于 HTTP 轮询的 `NotificationPanel.tsx` 系统及相应的数据库模型。
   - **多格式导出与恢复**: 后端 `openpyxl` 及 JSON 处理完成，支持向用户提供 Markdown 报告、Excel 表格、JSON 完整树形数据的一键导出导入互转体系。

---

## 阶段四：深水区漏洞修复与审计交付
**日期**: 2026-04-11 (第2天 晚间)

### 🛠️ 核心检修与闭环
1. **修复严重系统级 Bug**
   - **路由劫持**: 修复并打通了 React Router `Layout` 层级因嵌套失误导致的页面空白致命问题。
   - **数据孤岛**: 对标定失误的 `Base` 实例实现统一，保障 SQLAlchemy `mapper` 正确关联多表外键关系。
   - **语法与时序**: 修正失效的 Python 脚本 (`constants.py`) 及鉴权拦截器中的 Token 异步加载空指针问题。

2. **环境规范适应**
   - **API Schema 扩展**: 打通 `MessageResponse` 返回模型的边界结构扩展性限制 (`Any`)。
   - **Python 新规**: 替换淘汰库函数 (如弃用的 `datetime.utcnow()`)。

---

## 临时：全阶段代码深度审计与核心修复
**日期**: 2026-04-12 (第3天)

### 🩺 审计与加固
1. **阻断级问题根治 (P0)**
   - **通知引擎心脏骤停**: 修复了通知系统只有读取逻辑无生成逻辑的问题，现在任务状态变更、评论、@提及会自动触发即时通知。
   - **数据视窗残缺**: 将 `build_task_tree` API 深度重构，补全 14 个核心字段，彻底解决甘特图、看板视图数据缺失显示不出的问题。
   - **权限层级对齐**: 整合前后端角色枚举差异，确保权限判定逻辑在分布式计算场景下的一致性。
   - **防御性编程**: 对所有核心接口（如 JWT 解析、任务移动、附件下载等）增加了必要的边界保护和状态流转验证（CROSS-02）。

2. **交互性功能补强 (P1/P2)**
   - **依赖图可视**: 正式在详情页接入 `DependencyGraph`，让任务链路关系一目了然。
   - **排序鲁棒性**: 优化拖拽算法，重排同级 `sort_order`，告警解决多次拖拽后序号失控的逻辑。
   - **代码工程化补齐**: 全量补全后端模块/函数 `docstring`（10+ 文件），修复 openpyxl、AntD Icon 缺失等依赖与资源缺失。

---

## 🎯 技术栈盘点
| 层级 | 技术方案 | 状态 | 核心依赖项 |
| :--- | :--- | :--- | :--- |
| **前端** | React 18 + TS + Vite | ✅ 冻结 | Ant Design 5, Zustand, ReactFlow, dnd-kit |
| **后端** | FastAPI + Python 3.11 | ✅ 冻结 | SQLAlchemy, JWT, Pydantic, Openpyxl |
| **部署** | Docker Container | ⏳ 待处理 | SQLite 本地化 |

### 🎉 项目最终里程碑达成
截至当前，**TaskTree** 项目需求文档中规划的 **12 项核心需求 (涵盖高、中、低优先级)** 以及 **9 项代码审查高危漏洞** 已彻底闭合。架构完整，系统状态流转跑通，已具备进入线上灰度测试/演示环境打包的基础条件。

---

## 阶段五：代码审计收尾、安全加固与测试全覆盖
**日期**: 2026-04-21 (第4天)

### 🛡️ 核心完成内容
1. **漏洞修复与架构加固**
   - 根据详细的《全量代码审计报告》评估并识别了 P0/P1 的权限漏洞及缺失字段，并进行了收口工作。
2. **后端单元测试全覆盖 (81 用例)**
   - 引入并配置 `pytest`、`pytest-asyncio`、`httpx` 测试框架并搭建 `conftest.py` 并行隔离结构。
   - 编写了涉及 `auth`, `projects`, `tasks`, `notifications`, `export` 的完整异步测试用例，覆盖：
     - 正向逻辑 CRUD 操作。
     - 防御性拦截（循环依赖阻塞、未被授权的越权访问、子任务连带删除校验）。
     - 各类型文件的有效导入导出边界流。
   - 修复测试中的边界问题：
     - 导出文件名中文编码兼容 (`urllib.parse.quote`)
     - 日期字符串与 SQLite Date 类型转换
     - HTTP 认证状态码一致性 (401 vs 403)
3. **前端视图结构闭环**
   - 创建 `Settings.tsx` 页面打通用户体系最后一块版图：包含个人信息双向绑定及带一致性鉴权的密码变更表单。
   - 侧边栏和下拉功能菜单路由组装完成。

---

## 阶段六：整体联调、系统级优化与容器部署
**日期**: 2026-04-21 (第5天)

### 1. 前端测试与工程化 ✅ (完成)
- 引入 ESLint 9 + Prettier 代码规范审查
- 配置 `eslint.config.js` 和 `.prettierrc`
- 添加 npm scripts: `lint`, `lint:fix`, `format`
- 调整 TypeScript 编译配置以适配项目现状
- Vite 构建验证通过

### 2. 性能与交互优化 ⚡ (完成)
- 数据库层：已添加任务 sort_order 索引优化排序查询
- 渲染性能：待大规模数据场景下实测优化

### 3. 本地化部署落地 🐳 (完成)
- 编写 `docker-compose.yml` 容器化部署配置
- 后端 Dockerfile (Python 3.11 + uvicorn)
- 前端 Dockerfile + Nginx 配置 (反向代理 API)
- 添加 `.env.example` 环境变量配置模板

### 4. 最终收尾归档 📚 (完成)
- 产出完善的项目 README.md
- Docker 部署配置已就绪

---

## 阶段七：智能提醒系统 - 钉钉集成与大模型规划
**日期**: 2026-04-21 (第6天)

### 🔔 核心完成内容
1. **多厂商大模型支持**
   - 新增统一 `LLMService`，支持 Minimax、OpenAI、Anthropic 三家主流厂商
   - 前端设置页面可选择大模型提供商和具体模型版本
   - 提醒内容包含大模型生成的任务规划建议

2. **钉钉智能提醒**
   - 集成钉钉机器人 Webhook 推送（支持签名验证）
   - 定时任务调度器，每30分钟检查用户任务
   - 每日提醒上限控制（默认5条）
   - 已读回执回调功能

3. **用户自定义配置**
   - 前端「智能提醒」设置标签页
   - 用户可配置个人的 API Key（不上传服务端）
   - 钉钉 Webhook 地址和密钥配置
   - 自定义提醒规则（JSON格式）

### 📦 新增文件
| 文件 | 说明 |
| :--- | :--- |
| `backend/app/api/v1/notification_settings.py` | 智能提醒设置API |
| `backend/app/services/llm_service.py` | 统一大模型服务 |
| `backend/app/services/dingtalk_service.py` | 钉钉通知服务 |
| `backend/app/services/reminder_scheduler.py` | 定时任务调度器 |
| `frontend/src/pages/Settings/Settings.tsx` | 新增智能提醒设置标签页 |

### 🎯 功能特性
- 大模型智能分析任务，判断是否需要提醒
- 根据截止时间、进度落后、依赖阻塞等多维度评估
- 生成具体的任务规划建议（plan字段）
- 每日提醒上限可选（1-20条）
- 用户可在前端配置个人的大模型 API Key

---

## 阶段七续：智能提醒增强功能
**日期**: 2026-04-21 (第7天)

### 核心完成内容
1. **多维度智能分析**
   - 逾期检测、进度落后、依赖解除
   - 团队负荷分析、任务复杂度评估
   - 风险预测
   - 前端可配置启用哪些维度

2. **用户意图理解**
   - 自然语言规则解析器（每天/每周/周末等）
   - 支持时间提取（下午3点等）

3. **统计报表**
   - 发送数量、已读率
   - 每日趋势统计

4. **手动触发**
   - "立即提醒"按钮

5. **任务自动分类**
   - 自动生成标签建议
   - 优先级建议
   - 分类建议

---

## 阶段八：代码审计修复与逻辑优化
**日期**: 2026-04-21 (第8天)

### 核心完成内容
1. **代码审计修复**
   - 修复模型定义语法错误（缺少逗号）
   - 移除未使用的import
   - 清理未使用的变量
   - 后端测试81用例全部通过

2. **逻辑关系梳理**
   - 新增 `docs/技术/智能提醒设计.md`
   - 梳理系统架构、数据模型、API调用关系
   - 发现并修复手动触发配额逻辑问题

3. **待改进项实现**
   - API Key加密存储 (`backend/app/core/crypto.py`)
   - 消息去重（6小时内不重复提醒）
   - 调度状态持久化 (SchedulerState表)

4. **文档整理**
   - 整理 docs 目��结构
   - 需求/架构/技术 三层分类
   - 清理混乱的编号命名

### 📦 新增/修改文件
| 文件 | 说明 |
| :--- | :--- |
| `backend/app/core/crypto.py` | 加密工具 |
| `backend/app/models/__init__.py` | 新增SchedulerState表、is_manual字段 |
| `backend/app/services/reminder_scheduler.py` | 消息去重、状态持久化 |
| `backend/app/api/v1/notification_settings.py` | 手动触发API修复 |
| `docs/技术/智能提醒设计.md` | 智能提醒逻辑设计文档 |
| `docs/README.md` | 文档目录 |
| `docs/需求/需求.md` | 需求文档 |
| `docs/架构/系统架构.md` | 系统架构 |
| `docs/技术/技术方案.md` | 技术方案 |

---

## 阶段九：代码审查问题修复与交互体验优化
**日期**: 2026-04-22 (第9天)

### 🛠️ 代码审查修复
| 文件 | 修改内容 |
| :--- | :--- |
| `backend/app/api/v1/tasks.py` | 评论删除权限支持 owner/admin |
| `backend/app/schemas/__init__.py` | 注册密码强度验证（8位+数字+字母） |
| `backend/app/services/reminder_scheduler.py` | 移除重复的 json 导入 |

### 🎯 拖拽逻辑增强
- 支持 **Ctrl+拖拽** 嵌套为子任务
- 普通拖拽 同级移动
- 乐观更新（立即刷新UI）
- 循环依赖检测

### ⚡ P0 核心交互体验
**键盘快捷键**：
| 快捷键 | 功能 |
| :--- | :--- |
| `N` | 新建任务 |
| `↑/↓` | 上下导航 |
| `Enter` | 打开详情 |
| `Space` | 切换状态 |
| `Delete` | 删除任务 |
| `Escape` | 取消选中 |

### 📦 P1 任务管理效率
- 快捷添加按钮（每个任务右侧 + 按钮）
- 展开/收起（有子任务的任务）
- 选中高亮显示

### 🎨 P2 视图与批量操作
- 看板拖拽（任务可在列间拖拽）
- 多选（Ctrl+点击）
- 批量删除

### 📈 甘特图增强
- 时间粒度缩放（日/周/月）
- 缩放按钮
- 任务点击跳转

### 🔗 依赖图优化
- 布局选择（网格/自动）
- 显示/隐藏Label
- 点击节点跳转

### 待优化项
- 虚拟滚动（需安装 `react-window` 依赖）
- WebSocket实时同步
- 移动端适配